AuthType Basic
AuthName "RealmForge Admin"
# Update this path to match your server's .htpasswd location.
# Create the file with: htpasswd -c /path/to/.htpasswd admin
AuthUserFile /etc/realmforge/.htpasswd
Require valid-user
